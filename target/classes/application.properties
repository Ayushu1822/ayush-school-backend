# Database Connection Settings (Using dynamic environment variables with public fallbacks)
spring.datasource.url=jdbc:mysql://${MYSQLHOST:tokaido.proxy.rlwy.net}:${MYSQLPORT:28683}/${MYSQLDATABASE:railway}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:mDaxzXIvAqJpPIaRUYBrDkzLHIPYmJSS}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Port (Required for dynamic port assignment)
server.port=${PORT:8080}